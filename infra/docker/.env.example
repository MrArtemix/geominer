# =========================
# Ge O'Miner - Environment Variables
# =========================

# PostgreSQL + PostGIS
POSTGRES_USER=geominer
POSTGRES_PASSWORD=geominer_secret_2024
POSTGRES_DB=geominerdb
POSTGRES_PORT=5432

# Redis
REDIS_PASSWORD=redis_secret_2024
REDIS_PORT=6379
REDIS_MAXMEMORY=512mb

# MinIO (S3-compatible)
MINIO_ROOT_USER=geominer-admin
MINIO_ROOT_PASSWORD=minio_secret_2024
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001

# Keycloak
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=keycloak_secret_2024
KEYCLOAK_PORT=8080
KC_DB=postgres
KC_DB_URL=jdbc:postgresql://postgres:5432/keycloak
KC_DB_USERNAME=geominer
KC_DB_PASSWORD=geominer_secret_2024

# Mosquitto MQTT
MQTT_PORT=1883
MQTT_WS_PORT=9883

# Prefect
PREFECT_PORT=4200
PREFECT_API_URL=http://prefect-server:4200/api

# API Gateway
API_GATEWAY_PORT=8000
API_GATEWAY_HOST=0.0.0.0

# MineSPOT AI Service
MINESPOTAI_PORT=8001
MINESPOTAI_HOST=0.0.0.0

# AlertFlow Service
ALERTFLOW_PORT=8002

# AquaGuard Service
AQUAGUARD_PORT=8003

# GoldTrack Service
GOLDTRACK_PORT=8004

# LegalVault Service
LEGALVAULT_PORT=8005

# JWT / Keycloak
KEYCLOAK_REALM=geominer
KEYCLOAK_URL=http://keycloak:8080
KEYCLOAK_CLIENT_ID=geominer-api
KEYCLOAK_CLIENT_SECRET=change-me-in-production

# Database URL
DATABASE_URL=postgresql://geominer:geominer_secret_2024@postgres:5432/geominerdb

# Redis URL
REDIS_URL=redis://:redis_secret_2024@redis:6379/0

# MinIO
MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=geominer-admin
MINIO_SECRET_KEY=minio_secret_2024
MINIO_SECURE=false

# MQTT
MQTT_BROKER=mosquitto
MQTT_BROKER_PORT=1883
