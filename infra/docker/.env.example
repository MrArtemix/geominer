# =============================================================
# Ge O'Miner - Variables d'environnement
# Copier vers .env et adapter si necessaire
# =============================================================

# --- PostgreSQL / TimescaleDB ---
POSTGRES_USER=geominer
POSTGRES_PASSWORD=geominer2026
POSTGRES_DB=geominerdb
POSTGRES_PORT=5432
POSTGRES_MULTIPLE_DATABASES=geominerdb,keycloakdb,mlflowdb

# --- Redis ---
REDIS_PASSWORD=redis_secret_2024
REDIS_PORT=6379
REDIS_MAXMEMORY=512mb

# --- MinIO (S3-compatible) ---
MINIO_ROOT_USER=geominer
MINIO_ROOT_PASSWORD=geominer2026
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001

# --- Keycloak (SSO/OIDC) ---
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin2026
KEYCLOAK_PORT=8080
KEYCLOAK_REALM=geominer
KEYCLOAK_URL=http://keycloak:8080
KEYCLOAK_CLIENT_ID=geominer-api
KEYCLOAK_CLIENT_SECRET=change-me-in-production

# --- MQTT (Mosquitto / IoT AquaGuard) ---
MQTT_PORT=1883
MQTT_WS_PORT=9883
MQTT_BROKER=mosquitto
MQTT_BROKER_PORT=1883

# --- Prefect (Pipeline orchestration) ---
PREFECT_PORT=4200
PREFECT_API_URL=http://prefect-server:4200/api

# --- MLflow (ML tracking) ---
MLFLOW_PORT=5000

# --- Vault (Secrets management) ---
VAULT_PORT=8200
VAULT_DEV_ROOT_TOKEN_ID=geominer-vault-token

# --- IPFS (Kubo) ---
IPFS_API_PORT=5001
IPFS_GATEWAY_PORT=8081

# --- API Gateway ---
API_GATEWAY_PORT=8000
API_GATEWAY_HOST=0.0.0.0

# --- MineSPOT AI Service ---
MINESPOTAI_PORT=8001
MINESPOTAI_HOST=0.0.0.0

# --- AlertFlow Service ---
ALERTFLOW_PORT=8002

# --- AquaGuard Service ---
AQUAGUARD_PORT=8003

# --- GoldTrack Service ---
GOLDTRACK_PORT=8004

# --- LegalVault Service ---
LEGALVAULT_PORT=8005

# --- URLs derivees ---
DATABASE_URL=postgresql://geominer:geominer2026@postgres:5432/geominerdb
REDIS_URL=redis://:redis_secret_2024@redis:6379/0
MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=geominer
MINIO_SECRET_KEY=geominer2026
MINIO_SECURE=false

# --- Feature Flags (MVP) ---
USE_MOCK_BLOCKCHAIN=true
USE_IPFS_FALLBACK_MINIO=true
ENABLE_REAL_SENTINEL=false
