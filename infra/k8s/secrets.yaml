apiVersion: v1
kind: Secret
metadata:
  name: geominer-secrets
  namespace: geominer
  labels:
    app.kubernetes.io/name: geominer
type: Opaque
# IMPORTANT: Remplacer les valeurs par des secrets reels en production
# Utiliser: echo -n 'value' | base64
stringData:
  POSTGRES_USER: "geominer"
  POSTGRES_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
  REDIS_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
  MINIO_ROOT_USER: "geominer"
  MINIO_ROOT_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
  MINIO_ACCESS_KEY: "geominer"
  MINIO_SECRET_KEY: "CHANGE_ME_IN_PRODUCTION"
  KEYCLOAK_ADMIN: "admin"
  KEYCLOAK_ADMIN_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
  KEYCLOAK_CLIENT_SECRET: "CHANGE_ME_IN_PRODUCTION"
  VAULT_DEV_ROOT_TOKEN_ID: "CHANGE_ME_IN_PRODUCTION"
